import type { MainWindow, PartytownConfig } from '../types';
export declare const mainWindow: MainWindow;
export declare const docImpl: Document;
export declare const config: PartytownConfig;
export declare const libPath: string;
